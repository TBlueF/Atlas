import * as Atlas from "./atlas"
import * as THREE from "three"

/*
export * from "./lib"

declare global {
    interface Window {
        Atlas: typeof Atlas;
        THREE: typeof THREE;
    }
}

window.Atlas = Atlas;
window.THREE = THREE;
*/